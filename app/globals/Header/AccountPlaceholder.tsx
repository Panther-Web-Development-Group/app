import type { FC } from "react"

/** Reserved for future account navigation; hidden visually but kept for layout parity. */
export const AccountNavPlaceholder: FC = () => (
  <div className="navbar-account-nav" aria-hidden="true" />
)
